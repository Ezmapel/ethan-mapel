// JavaScript and the Browser
// Chrome is essentially THE browser to use, unless you are using CSS Grid...then Firefox has excellent dev tools
// LIKELY INTERVIEW QUESTION: "Stepping through a function" means go through function and debug it line by line - use dev tools to create a breakpoint (stop all code after that) and then walk through it
const kitten = "";
const cat = () => {
  console.log(kitten);
};

cat();
